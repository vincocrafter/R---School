# a
a <- 17

# vecta wird a zugewiesen
vecta <- a

# while wird ausgeführt
while(sum(vecta) <= 40){
	# a wird a + 1 zugewiesen
	a <- a+1
	# vecta wird veta und a zugewiesen
	vecta <- c(vecta, a)
}
# vecta wird ausgegeben
vecta